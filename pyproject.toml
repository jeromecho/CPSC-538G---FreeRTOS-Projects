[project]
name = "freertos-project"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "pandas>=3.0.1",
    "plotly>=6.5.2",
    "pyserial>=3.5",
]

[tool.ruff]
line-length = 200